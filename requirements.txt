# Pinned to the exact versions validated locally so Streamlit Cloud builds the
# same, tested environment. (Unpinned `>=` previously let the cloud pull brand-new
# majors like pandas 3.x / yfinance 1.x on a fresh build, which can break deploys.)
# To upgrade: bump here, re-test locally + `python backtest.py`, then redeploy.

# Core data + numerics
pandas==3.0.3
numpy==2.4.6
yfinance==1.4.1         # free price + fundamentals (pluggable; swap for paid provider later)

# Dashboard
streamlit==1.58.0
plotly==6.8.0
matplotlib==3.10.9      # required by pandas Styler.background_gradient (table shading)

# News overlay (Phase 2) — all free / open source, no API keys
vaderSentiment==3.3.2   # lexicon sentiment, pure-Python, no model download
lxml==6.1.1             # for pandas.read_html (S&P 500 constituent scrape)

# Utilities
python-dateutil==2.9.0.post0
requests==2.34.2
curl_cffi==0.15.0       # browser-impersonating session for resilient yfinance fetches

# --- Optional / later phases (not required for the MVP) ---
# pandas-ta-classic    # 190+ technical indicators (Python 3.10-3.12 friendly)
# pyqlib               # Microsoft Qlib factor/ML backbone (needs its own venv)
